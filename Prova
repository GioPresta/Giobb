Modifica queste due righe nel codice:

**Riga ~150 (foglio Finale):**
```python
# CAMBIA QUESTA:
ws_finale.write_row(1, 0, [f"Column{i+1}" for i in range(len(df_sheet1.columns))])

# CON QUESTA:
original_column_numbers_sheet1 = []
for col_name in df_sheet1.columns:
    if col_name in df_sheet2.columns:
        pos_in_sheet2 = list(df_sheet2.columns).index(col_name)
        original_col_num = columns_requested[pos_in_sheet2]
        original_column_numbers_sheet1.append(f"Column{original_col_num}")
ws_finale.write_row(1, 0, original_column_numbers_sheet1)
```

**Riga ~170 (foglio COD_FISC_CONTRAENTE):**
```python
# CAMBIA QUESTA:
ws_completo.write_row(1, 0, [f"Column{i+1}" for i in range(len(df_sheet2.columns))])

# CON QUESTA:
original_column_numbers_sheet2 = [f"Column{col_num}" for col_num in columns_requested[:len(df_sheet2.columns)]]
ws_completo.write_row(1, 0, original_column_numbers_sheet2)
```